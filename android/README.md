# Daylight DC1 viewer

Milestone 4. Native Android app: read-only view of Claude sessions (terminal, branch commits
with own-session highlighting, PR view), agent-pushed HTML in a WebView, YubiKey Bio escalation
for privileged actions (milestone 5).

Not started. See `../spikes/yubikey-android/` for the hardware assumption test.
